/**
 * Infrastructure adapters: JPA persistence, external HTTP clients, messaging, storage,
 * and other framework-specific integrations.
 * <p>
 * Concrete implementations that currently live under {@code entity}, {@code repository},
 * and parts of {@code config} may migrate into subpackages here over time.
 */
package com.agenciahub.api.infrastructure;
